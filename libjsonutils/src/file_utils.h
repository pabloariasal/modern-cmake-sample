#pragma once

#include <string>

namespace jsonutils
{
    inline bool doesFileExist(const std::string& file)
    {
        //Simulate that we are checking file existence
        return true;
    }
}
